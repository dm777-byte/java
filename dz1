3. 
public static void main(String[] args) {
        float a = 2;
        float b = 3;
        float c = 4;
        float d = 5;
        System.out.println(a * (b + (c / d)));

В результате компиляции выводится значение: 7.6.
Для того чтобы сделать возрат результата нужно добавить:
        int result = a * (b + (c / d));
        return result;
Но компиплятор не работает, выдаёт ошибку.

4.  
public static void main (String[] args) {
System.out.println("dz4.");
boolean a = true;
boolean b = false;
int sum = a + b
if (sum > 10 && sum < 20) return true;
else return false;
